# Points the mobile app at the Next.js API. Copy to mobile/.env and adjust
# for your environment — use your machine's LAN IP (not localhost) when
# testing on a physical device or simulator that isn't sharing your host's
# network stack.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
